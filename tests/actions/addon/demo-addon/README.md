# demo-addon

A complete addon used by the addon tests, it provides all the payload kinds
and uses the optional `src` layout: only `src/` is installed, this README and
the `tests` directory are ours and must not be installed.
