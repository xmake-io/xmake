#define HAVE_${module}_H 
#define HELLO "${hello} ${ARCH} ${PLAT}"

${define FOO_ENABLE}
${define FOO_ENABLE2}
${define FOO_STRING}

${define FOO2_ENABLE}
${define FOO2_ENABLE2}
${define FOO2_STRING}
